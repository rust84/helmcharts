apiVersion: v1
kind: Service
metadata:
  name: {{ include "grafana-image-renderer.fullname" . }}
  labels:
{{ include "grafana-image-renderer.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8081
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "grafana-image-renderer.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
